/**
 *    author:  Taara Sinh Aatrey
 *    created: 10.12.2022 20:40:26
**/

#include "bits/stdc++.h"

using namespace std;

#ifdef AATREY_DEBUG
#include "debug.hpp"
#else
#define dbg(...)
#endif

#define int int64_t

signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n;
    cin >> n;
    while (n >= 0) {
        cout << n << "\n";
        n--;
    }
    return 0;
}